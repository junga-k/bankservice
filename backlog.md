# backlog.md

아직 처리 안 된 작업 체크리스트. 완료하면 체크하고, 상세 조사/시행착오 기록은 `session-log.md`에 남긴다(이 파일엔 "무엇을 해야 하는지"만 간결하게 유지).

## Vercel/Turso 마이그레이션 후속 — 별도 인프라 결정 필요 (2026-07-31 추가, 우선순위 없음, 상세: `vercel-deploy-progress.md`)

- [ ] **Kafka 브로커 + `transfer_consumer.py` 컨슈머를 상시 호스팅할 곳 마련** — 지금은 `KAFKA_DISABLED=true`로 `/api/transfer`가 Kafka 없이 동기 폴백(`process_transfer()` 직접 호출)으로만 동작. 데이터 정확성은 검증됐지만 원래 설계였던 비동기 처리(빠른 "pending" 응답 + 폴링)는 못 씀.
  - Railway는 2026년 초부로 무료 티어 완전 폐지. Render 무료 티어는 15분 무활동 시 sleep이라 상시 TCP 연결이 필요한 Kafka 브로커엔 부적합. Fly.io도 무료 없어졌고, "상시 켜짐" 설정 시 카드 등록 + 월 ~$2.
  - Vercel은 서버리스라 `transfer_consumer.py`의 `while True` 루프를 못 돌림 — 브로커뿐 아니라 컨슈머가 상시 실행될 곳도 별도로 필요(Fly.io 등 한 곳에 브로커+컨슈머 같이 얹는 방안이 유력).
  - 대안(Confluent Cloud 등 관리형)은 SASL_SSL 인증 코드 추가가 필요하지만 큰 변경은 아님 — 어느 쪽이든 "상시 호스팅할 곳 마련"이 진짜 병목이라 오늘 범위 밖으로 미룸.
- [ ] **`process_transfer()`의 쓰기 쿼리(UPDATE/INSERT) 배치·병합** — 읽기 쿼리 3개는 LEFT JOIN으로 병합 완료(2026-07-31, `backend/db.py`), 쓰기 5~7개(잔액차감 UPDATE, 거래내역 INSERT ×1~2, 잔액증액 UPDATE, 거래내역 INSERT, 상태갱신 UPDATE)는 서로 다른 행을 건드리는 개별 DML이라 JOIN 불가. libsql이 `executescript()`도 미구현이라 배치 실행도 안 됨 — 스키마 변경(예: 트리거로 잔액 자동갱신) 또는 더 큰 리팩토링이 필요해서 보류.
- [ ] **리전 지연 완화(Turso/Vercel 리전 조정)** — Vercel 함수 리전(iad1, 미국 동부)과 Turso DB 리전(aws-ap-northeast-1, 아시아) 간 거리로 쿼리 1회당 300~650ms(로컬 실측치의 3~4배) 소요, `/api/transfer` 웜 상태 응답이 JOIN 병합 후에도 ~9초. 근본 해결은 Turso DB를 Vercel 함수와 같은 리전(또는 가까운 리전)으로 옮기거나, Vercel 함수 리전을 Turso 쪽(아시아)으로 맞추는 것 — 어느 쪽이든 기존 데이터/설정 재구성이 필요한 인프라 결정이라 오늘 범위 밖.
- [ ] **⚠️ AI은행원(RAG 챗봇)이 프로덕션에서 완전히 작동 안 함 — 위 3건보다 영향도 큼, 우선 검토 권장.** `site/js/main.js`의 `CHAT_URL = "http://localhost:8501/..."`가 하드코딩돼 있어, iframe이 방문자 "자기 자신의" localhost:8501을 가리킴 — 실제 방문자 브라우저엔 아무것도 없어서 빈 화면. (2026-07-31 시딩 검증 중 발견: 화면이 정상으로 "보였던" 건 테스트에 쓴 로컬 머신에 우연히 Streamlit이 떠 있어서였고, `lsof -i :8501`로 로컬 프로세스 확인 후 착각임을 확인함 — 실제 방문자 재현 아님.) Vercel 배포(`[tool.vercel] entrypoint = backend.app:app`)엔 애초에 Streamlit이 포함되지 않음 — Kafka와 동일하게 "Streamlit을 상시 호스팅할 곳 마련" + `CHAT_URL`을 환경변수/상대경로로 교체하는 두 가지가 다 필요. `CHAT_BASE_URL` 환경변수 배선 자체는 코드로 완료해 커밋함(`backend/app.py`의 `/js/env-config.js`, 커밋 `5088995`, 2026-08-03). 환경변수 미설정 시 기존과 동일하게 localhost:8501 폴백이라 이 커밋만으로는 프로덕션 동작 변화 없음 — Streamlit 상시 호스팅처 결정(저장소 public 전환 여부 등)만 남음.

## AI은행원 에이전트 개선 (2026-07-31 추가, 크래시/오답 아님 — 정확도 개선 항목)

- [ ] **에이전트가 "문서(서식·약관·설명서)" 관련 질문에 `tool_get_documents`를 안 부름** — "예금자보호 한도가 얼마예요?"처럼 `documents` 테이블에 정확히 대응하는 문서("예금자보호법 안내문")가 있는 질문인데도, 에이전트가 `tool_get_faqs`만 시도하고(당연히 못 찾음) `tool_get_documents`는 아예 호출하지 않은 채 LLM 일반 지식으로 답함(최대 5천만원 — 결과적으로 정답이었지만 우리 문서를 근거로 한 답은 아니었음). **크래시나 오답이 발생하는 버그는 아니고, 이미 시드된 정확한 근거 문서가 있는데도 활용을 못 해 "더 정확할 수 있는데 일반 지식으로 폴백되는" 정확도 개선 항목**. `backend/db.py`의 검색 매칭 자체는 정상(2026-07-31 수정 후 `/api/documents?q=예금자보호` 1건 정상 반환 확인) — 문제는 에이전트의 도구 선택(`agent.py`의 시스템 프롬프트/도구 설명) 쪽일 가능성이 큼.

## AI은행원 UI — 로딩 인디케이터 (2026-08-06, 완료)

- [x] ~~stSpinner 재스킨의 회색 링 한계를 커스텀 아이콘으로 해결~~ (완료, 2026-08-06) — 2026-08-05(계속 6)에 남겨뒀던 "스피너 링 자체는 재색상이 안 먹혀 회색 그대로 둔 한계"를 매치뱅크 로고(겹친 두 원)를 재사용한 방사형 회전 아이콘으로 대체. 4단계 디자인 반복(잎사귀→로고 원 궤도→정지 호흡→위치 이동→최종 회전, 매번 Artifact로 확인) 끝에 "두 원의 상대 위치·크기 고정, 쌍 전체를 중심축으로 1.8초/360° linear 회전"으로 확정. `app.py`의 에이전트 경로(`st.spinner`→`st.empty` placeholder)와 스트리밍 경로(`next()`로 첫 청크 확인 후 placeholder 교체) 둘 다 적용.
  - **버그 발견·수정 1**: `<symbol>`+`<use>` 방식으로 만들면 `<use>` 그림자 트리 안의 CSS 애니메이션이 실제로 재생되지 않음(`document.getAnimations()`로 확인) — `<svg>`에 매번 인라인으로 그리는 방식으로 교체, 회전 각도가 시간에 정확히 비례함을 재확인.
  - **버그 발견·수정 2**: "새 채팅" 추천 칩을 숨기는 CSS가 `_pending_input` 조건부 블록 안의 `st.markdown("<style>")`에 있어서, 메시지 제출 순간 그 스타일시트 자체가 DOM에서 사라져 칩이 기본값(`display:flex`)으로 잠깐 잔상처럼 보이는 버그 — 해당 CSS를 최상단 전역 스타일 블록으로 이동해 해결.
  - 상세: `session-log.md` `### 2026-08-06`.

## Figma 디자인시스템 구축 — 다음 착수 항목부터

파일: `https://www.figma.com/design/KnJEcMeU05dCaVk3krDKKF`. 상태 원장: `/tmp/dsb-state-matchbank-001.json`(컬렉션/컴포넌트/화면 노드ID 정리돼 있음 — 세션 시작 시 먼저 읽을 것). 토큰 표: `docs/tokens.md`.

- [x] ~~컨테이너 폭 실측 수정~~ (완료)
- [x] ~~컴포넌트 11개 실측 재검증~~ (완료, `docs/tokens.md` 참고)
- [ ] **BO_UsageStats kpi-row(`47:36`)/panels-row(`47:49`)의 32px 인셋이 실제 CSS(인셋 없음, 1180px 전체)와 불일치** — TOP5 패널 작업(2026-07-30) 중 발견. 다른 Backoffice 화면(BO_Products 등)에도 같은 32px 인셋 관행이 있는지 먼저 확인 후 일괄 수정 필요
- [ ] **Backoffice `admin-table`의 `table-header` 배경색이 전 화면 공통으로 `--border`(#DDE3EA)를 쓰고 있음, 실제 CSS는 `--bg-soft`(#F8FAFD)** — 2026-08-04 Backoffice 서브 칩 작업 중 발견(Products/Members 등에서 동일하게 재현 확인). 범위가 넓어(대시보드·회원관리·이체모니터링·이용통계·성능관리·프롬프트관리·상품관리·FAQ관리·배너관리 전 테이블) 이번엔 기존 관행을 그대로 따르기만 하고 수정은 보류 — 일괄 수정하려면 먼저 전체 테이블 목록부터 스캔 필요
- [ ] **FAQ·공지사항관리 "공지사항 관리" 패널에 검색바(`bo-search-inline`) 누락** — 실제 웹엔 제목/내용 검색창+검색버튼이 있는데 Figma엔 없음(2026-08-04 발견, 같은 날 새로 만든 FAQ/이벤트/문의내역 3개 탭엔 반영함). 공지사항 탭만 남은 상태
- [x] ~~`Screens / Support` "문의하기"(`298:2`) 화면에 실제로는 없는 검색창이 남아있음~~ (완료, 2026-08-04) — `support-topbar`(`298:23`)에서 검색창 인스턴스(`344:453`) 제거. `SPACE_BETWEEN` 2자식 구조라 제거 후 탭만 좌측에 남는 것 스크린샷으로 확인.
- [x] ~~아이디찾기/비밀번호찾기 페이지 내용 미완성~~ (완료, 2026-08-04) — `Screens / Find ID`(`72:3`)/`Screens / Reset Password`(`72:4`)에 Login 화면(`38:2`) 구조(브랜드패널 560/카드 880, card-logo 등)를 복제해 실제 웹 폼 그대로 반영. Find ID: 이름+이메일(인증번호 받기 버튼 포함)+비활성 제출버튼. Reset Password: 2단계 스텝 인디케이터(`1 본인확인` 활성/`2 새 비밀번호`)+STEP 1 필드 4개(아이디/이름/전화번호/이메일)+비활성 "다음" 버튼 — STEP 2(새 비밀번호 입력)는 회원가입 2·3단계와 같은 이유로 이번 스코프에서 제외(아래 항목에 포함). 웹 스크린샷과 나란히 비교해 구조·타이포·색상 일치 확인.
  - [x] ~~부수 발견 — 브랜드패널 `.auth-aside-stats` 누락~~ (완료, 2026-08-04) — Login/Find ID/Reset Password 3개 화면 브랜드패널에 통계 목록(16개 은행/5종 금융상품/금융감독원 공식 데이터) + 상단 구분선 일괄 추가. desc→통계 간격이 48(desc `margin-bottom`)로 다른 두 간격(24)과 달라 `itemSpacing` 균일값으로는 표현 불가 — Products 컨테이너 때와 동일한 방식(`itemSpacing:0`+개별 spacer 프레임 24/24/48)으로 재구성.
- [x] ~~회원가입 2단계(계좌등록+이체비밀번호+약관동의)~~ (완료, 2026-08-04) — `Screens / Signup` 페이지에 `Signup (Step 2)`(`248:2`) 신규 생성(`Signup (Step 1)`=`73:2` 옆에 배치). 계좌 등록(은행선택 Select+계좌번호+예금주명 1원인증 verify-row+계좌별칭(선택)+대표계좌 체크), 이체 비밀번호, 약관동의 4행(필수 3+선택 1, "보기" 링크), 하단 이전/회원가입 버튼까지 실제 웹 그대로 반영. 카드 높이가 1004px로 커져서 프레임/브랜드패널도 840→1084로 재조정.
  - **부수 수정**: Step 1의 스텝 인디케이터(`1 정보입력/2 계좌·약관/3 완료`)가 원래 텍스트 색상만 다른 민무늬였던 걸 발견 — Reset Password 때 만든 방식(알약형 배경, active/done/default 3-state)으로 Step 1도 함께 통일, Step 2는 처음부터 이 방식으로 제작(1=done, 2=active, 3=default).
  - **겪은 버그 2건**: (1) 카드가 auto-layout hug 모드인데 여러 차례의 `use_figma` 호출에 걸쳐 자식을 계속 추가하니 `height` 값이 중간 상태(760)에서 갱신이 안 되고 고정되는 현상 발견 — `resize()`로 강제 개입해야 실제 콘텐츠 높이(1004)로 재계산됨(Support 행 720px 오보고와 같은 종류의 스테일 캐시). (2) 하단 버튼 2개를 `layoutSizingHorizontal="FILL"` 세팅 직후 `resize()`를 또 호출해 폭이 1px로 찌그러짐 — `resize()`가 사이징모드를 FIXED로 되돌리는 룰 그대로 재현된 사례, "resize 먼저 → FILL은 맨 마지막에 한 번만" 순서로 재수정.
  - 웹 스크린샷과 나란히 비교(스텝 색상/필드 구성/체크박스/버튼 전부 일치) 확인.
- [x] ~~회원가입 3단계(완료 화면)~~ (완료, 2026-08-04) — `Signup (Step 3)`(`273:2`, x=3080) 신규 생성. Step 2 clone 후 폼 필드 전부 제거하고 card-logo+"회원가입" 제목+스텝(1·2=done/3=active)만 유지, 그 아래 `auth-welcome` 블록(체크 배지 "회원가입 완료"+`icon/check` 13px, "환영합니다!" 22px, 설명 16px, 전체폭 "홈으로 가기" 버튼) 추가. 카드가 훨씬 짧아져서(362px) 프레임도 760으로 축소. 실제 웹과 스크린샷 대조 일치 확인. 이걸로 회원가입 1~3단계 전부 완성.
- [x] ~~Reset Password STEP 2(새 비밀번호 입력)~~ (완료, 2026-08-04) — `Reset Password (Step 2)`(`276:2`, x=1540) 신규 생성. Step 1 clone 후 필드 4개 전부 제거하고 card-logo+제목+스텝(1=done/2=active)만 유지, 새 비밀번호+새 비밀번호 확인(힌트 "4자 이상 입력하세요.") + 이전/비밀번호 재설정(비활성) 버튼 추가. 실제 웹과 스크린샷 대조 일치 확인. 이걸로 아이디/비밀번호 찾기 + 회원가입 전체 인증 플로우 완성.
- [x] ~~내 계좌 "이체" 탭(이체 위저드 STEP 1: 정보입력)~~ (완료, 2026-08-04) — `Screens / Account` 페이지에 `Account - Transfer`(`260:41`) 신규 생성(기존 `Account`(`32:2`) 헤더/탭 구조 복제, "이체" 탭 Active로 스왑). 자주쓰는계좌 pill 목록, 3단계 스텝 인디케이터(1 정보입력 active), 출금계좌 select+잔액 hint, 받는 분 계좌(은행선택+계좌번호+확인 버튼 grid), 예금주 확인 결과, 이체금액(우측정렬 큰 글씨+원 단위)+빠른금액 pill 5개+이체 후 잔액 hint, 받는/보내는 분 통장표시(선택) 2개, 한도 안내 박스, 우측정렬 "다음" 버튼(비활성 아님, min-width 200)까지 실제 웹 그대로 반영.
  - **겪은 버그**: 이체금액 입력창에서 `layoutSizingHorizontal="FILL"` 설정 후 `resize()`를 또 호출해 폭이 1px로 찌그러지며 텍스트가 화면 밖으로 밀려 안 보이던 것 발견 — Signup STEP 2 하단 버튼 때와 완전히 같은 순서 실수(`resize` 먼저 → `FILL`은 마지막에) 재발, 같은 방식으로 수정.
  - 실제 웹(로그인 후 이체 탭 강제 노출)과 스크린샷 대조 후 출금계좌 select 텍스트("은행명+계좌번호 · 잔액 N원" 결합 표기)와 하단 hint 문구("출금가능 금액")가 실제와 다른 것 발견해 수정.
  - STEP 2(확인)/STEP 3(완료 영수증)는 이번 스코프에서 제외 — 아래 항목에 별도 기재.
- [x] ~~이체 위저드 STEP 2(확인)/STEP 3(완료)~~ (완료, 2026-08-04) — `Account - Transfer (Step 2)`(`265:2`)/`(Step 3)`(`270:2`) 신규 생성(각각 x=1540/3080에 배치). STEP 2: `tf-confirm-box`(cf-row 9줄+점선 구분선+출금합계 강조), 확인 체크박스(체크됨), 이체비밀번호 입력, 이전/이체실행 버튼. STEP 3: 체크마크 원(그림자 포함)+완료 문구+금액(30px)+거래상세 6줄+확인 버튼(중앙정렬).
  - **버그**: 스텝 인디케이터(1/2/3) 상태를 `makeStepPill()` 헬퍼로 만들었는데, 코드는 맞는데 실제 결과가 반대로 나옴(1번이 active색, 2번이 default색) — 원인 특정은 못했지만 스크린샷으로 발견 즉시 인스턴스에 직접 색상 재대입해서 수정. STEP 3부터는 헬�퍼 대신 직접 대입 방식으로 바꾸고 생성 직후 바로 스크린샷 확인하는 습관으로 재발 방지.
  - 실제 웹에서 STEP 2/3를 강제로 노출시켜 스크린샷 대조 — 스텝 색상, 확인박스 레이아웃, 영수증 디자인 전부 일치 확인.
- [x] ~~내 계좌: 계좌조회에서 "거래내역" 클릭 화면~~ (완료, 2026-08-04) — `Account - Transaction Detail`(`279:90`, y=1400) 신규 생성. 기존 `Account` 프레임 clone → account-grid 아래에 `acct-detail` 패널 추가(제목 "은행명 계좌번호 · 잔액", `tx-row` 7개: 날짜+거래상대방 좌측, 금액(색상)+잔액 우측). 신한은행 계좌 실제 거래내역(`/api/accounts/1/transactions`) 중 6건 그대로 사용 + 웹과 대조 중 출금(빨강)만 있고 입금(초록) 예시가 없는 걸 발견해 실제 데이터의 "급여 +2,000,000원" 건을 7번째 행으로 추가, 두 색상 상태 모두 반영.
- [ ] 화면 커버리지를 "메뉴칩/플로우 단계별로 전부" 확장
  - [x] ~~상품안내 카테고리 탭 5개~~ (완료, 2026-08-04) — `Products (적금)`(`284:83`)/`(주택담보대출)`(`287:2`)/`(전세자금대출)`(`288:2`)/`(신용대출)`(`288:1196`) 4개 신규 생성(예금은 기존 `34:2`). 각 화면 cat-tabs 활성상태 스왑 + 제목/설명/정렬라벨 + TOP추천 + 상품리스트 8행(은행배지/상품명/금리) 전부 실제 `/api/products?category=X` 데이터로 교체. **대출 3종(주택담보대출/전세자금대출/신용대출)은 예금/적금과 렌더링 자체가 달라** 금리가 범위(연 X%~Y%)로 표시되고 정렬도 "금리 낮은순"으로 반대 — 실제 `main.js`(`getProductRateRange`) 로직 확인 후 반영. 수협은행처럼 BankBadge 컴포넌트가 없는 은행은 기존 Fallback 프레임 패턴을 그대로 복제/이동해 재사용(신용대출 화면에서 실제로 한 행은 Fallback→실제뱅크, 다른 행은 실제뱅크→Fallback로 서로 바꿔치기 필요했음). 금리 텍스트가 범위로 길어져도 우측 정렬이 자동으로 유지되는 것 실측 확인(수동 재배치 불필요).
  - [x] ~~고객센터 FAQ/서식/문의내역 탭~~ (완료, 2026-08-04, 공지사항은 이미 완료) — `Support (FAQ)`(`295:18`)/`(서식·약관·설명서)`(`296:2`)/`(문의하기)`(`298:2`) 신규 생성. FAQ는 공지사항과 완전 동일한 `.faq-q` 아코디언 구조라 가장 단순(실제 `/api/faqs` 4건). 서식·약관·설명서는 새 구조(`.document-item`: 제목+분류배지(약관/서식/설명서)+설명, 아코디언 아님, 실제 `/api/documents` 5건). 문의하기는 로그인 필요 탭 + "문의 작성"(제목+내용 폼)/"내 문의 내역" 2패널 구조 — 문의내역은 실제 데이터 6건 중 자동화 테스트 흔적("PW 테스트 문의" 5건)을 빼고 깨끗한 실제 예시("이체 수수료 문의") 1건만 사용(의도적 선택).
  - **버그(재발)**: 문의하기의 문의내용 textarea에서 또 `layoutSizingHorizontal="FILL"` 설정 후 `resize()`를 호출해 폭 1px로 찌그러지며 placeholder 텍스트가 안 보이던 것 발생 — 이번 세션에서만 벌써 3번째 동일 실수, "resize 먼저 → FILL 마지막" 순서를 스크립트 작성 시 습관적으로 챙길 것.
  - 실제 웹과 스크린샷 대조(FAQ/서식/문의하기 3개 전부) — 레이아웃·데이터 일치 확인. "이벤트" 탭(backlog에 명시 안 됨)은 이번 스코프에서 제외 — 필요하면 별도 요청.
  - [ ] 마이페이지 각 섹션(7개, 순차 진행 중: 보안→계좌관리→즐겨찾기→예약이체→문의내역→거래명세서→회원탈퇴)
    - [x] ~~보안~~ (완료, 2026-08-04) — `MyPage (보안)`(`301:33`) 신규 생성. 이체비밀번호/오픈뱅킹동의/보안이벤트 요약 카드 3개, 로그인 비밀번호 변경 폼, 이체 비밀번호 변경 폼, 알림 수신 동의, 내 이체 한도(1회/1일/타행수수료/오늘사용액/잔여한도), 내 보안 이력(4건) — 전부 실제 API(`/api/me/security-overview` 등) 데이터로 구성.
    - [x] ~~계좌 관리~~ (완료, 2026-08-04) — `MyPage (계좌 관리)`(`304:2`) 신규 생성. 내 계좌 카드 3개(별칭 입력+저장/대표지정/해지 버튼), 계좌 추가 폼(은행선택+계좌번호+예금주명 1원인증+계좌별칭+대표계좌 체크). **버그**: 액션 버튼("별칭 저장"/"대표 지정"/"삭제")을 고정폭(80/80/60)으로 만들어 텍스트가 잘렸던 것 발견 → `layoutSizingHorizontal="HUG"`로 전환해 자연폭(125/125/93)으로 해결.
    - [x] ~~즐겨찾기~~ (완료, 2026-08-04) — `MyPage (즐겨찾기)`(`306:117`) 신규 생성. 실제 API(`/api/me/favorites`)가 데모 유저 기준 빈 배열이라, 두 상태(soft-active 탭 검증 포함) 시연을 위해 실제 API로 즐겨찾기 2건(이영희/국민은행, 김철수/우리은행)을 직접 등록(POST) 후 그 실데이터로 "자주 쓰는 계좌" 목록 구성. "즐겨찾기 추가" 폼(은행 Select+계좌번호+예금주명(선택)+별칭(선택)+추가 버튼)도 함께 제작.
      - **버그**: `mp-fav` 행을 만들 때 정보(`info`) 프레임엔 `layoutSizingHorizontal="FILL"`을 줬지만 행 자체(`row`)에는 안 줘서(기본 HUG) 이름/부제 텍스트가 거의 폭 0으로 찌그러져 안 보이는 문제 발생 — 부모(`row`)까지 FILL로 설정해야 자식의 FILL이 의미가 생긴다는 걸 재확인, 즉시 수정.
    - [x] ~~예약이체~~ (완료, 2026-08-04) — `MyPage (예약이체)`(`316:148`) 신규 생성. 실제 `/api/me/scheduled-transfers`가 빈 배열이라 실제 `/api/transfer`로 예약 1건(이영희, 미래 시각)+지연 1건(김철수, `delay_minutes`)을 직접 등록해 그 실데이터로 구성. `mp-sched-badge`(예약=info 파랑/지연=warn 주황) + 이름·계좌+시각 + 금액 + 취소 버튼.
    - [x] ~~문의내역~~ (완료, 2026-08-04) — `MyPage (문의내역)`(`318:164`) 신규 생성. 고객센터 "문의하기"의 "내 문의 내역"과 동일 데이터 소스(`/api/inquiries`) — 자동화 테스트 흔적 5건을 뺀 깨끗한 실제 예시 1건("이체 수수료 문의") 재사용, `mp-inquiry-head`(제목+날짜 space-between)+본문 구조로 구성.
    - [x] ~~거래명세서~~ (완료, 2026-08-04) — `MyPage (거래명세서)`(`320:198`) 신규 생성. 전체선택 체크박스+"PDF 다운로드" 버튼 툴바 + 3개 계좌 실거래 중 입금/출금·은행 3곳이 고루 섞인 대표 8건(체크된 상태, `--blue-soft` 배경) 리스트. **버그**: Primary Button 인스턴스 안에 다운로드 아이콘을 끼워 넣으려다 `insertChild: Cannot move node. New parent is an instance` 에러 발생(인스턴스는 임의 자식 삽입 불가) — 아이콘 없이 텍스트만 있는 버튼으로 대체(Button 컴포넌트에 아이콘 슬롯 자체가 없어 이번 스코프에서 아이콘 재현은 보류).
    - [x] ~~회원탈퇴~~ (완료, 2026-08-04) — `MyPage (회원탈퇴)`(`323:1297`) 신규 생성. 경고 문구 2줄 + 비밀번호 확인 필드 + "회원 탈퇴" 위험 버튼. Button 컴포넌트셋에 Danger variant가 없어 이 인스턴스 하나만 `fills`를 빨강(`#D93025`)으로 override(사용처가 이 화면 하나뿐이라 새 variant 신설 대신 인스턴스 오버라이드로 처리 — Chip의 8개 화면 영향과는 다른 판단).
    - **이걸로 마이페이지 7개 섹션(보안/계좌관리/즐겨찾기/예약이체/문의내역/거래명세서/회원탈퇴) 전부 완성.** 전 화면 실제 웹과 스크린샷 대조 검증 완료, 각 화면 위치 안내 캡션 추가.
  - [x] ~~Backoffice 각 탭 서브 칩(금융상품관리 4개+FAQ·공지사항관리 4개)~~ (완료, 2026-08-04)
    - **부수 발견 1 — 상품미리보기/인기상품통계 패널이 한 화면에 같이 있었음**: 기존 `Backoffice Products`(`50:2`)에 "상품 미리보기"(cat-tabs+table)와 "인기 상품 통계"(FSS 조회 통계+카테고리별 인기상품) 패널이 실제 웹은 탭별로 하나만 보이는데(`display:none` 토글) Figma엔 둘 다 같이 들어있었음 — 통계 패널을 분리해 `Backoffice Products (인기 상품 통계)` 새 프레임으로 옮기고, 원본은 미리보기 콘텐츠만 남도록 정리(둘 다 main/body/sidebar 높이 재조정).
    - **금융상품관리 4개**: 상품 미리보기(`50:2`, 정리됨) / 인기 상품 통계(`328:69`, 신규) / 서식·약관·설명서(`330:81`, 신규, 실제 문서 5건) / 특별상품 관리(`332:1359`, 신규, 실제 특별상품 1건). 서식 탭 칩 라벨이 원래 "서식·약관 관리"로 잘못돼 있던 것도 실제 웹 텍스트("서식·약관·설명서")로 수정.
    - **부수 발견 2 — FAQ·공지사항관리 공지사항 프레임에 수정/삭제 버튼 누락**: 기존 `notice-list`(`51:55`) 테이블이 제목/등록일 2열만 있고 실제 웹에 있는 수정/삭제 액션 버튼이 없었음 — 열 구조를 `SPACE_BETWEEN`에서 고정폭 3열(제목700/등록일100/액션180)로 바꾸고 Ghost 버튼 2개 추가.
    - **FAQ·공지사항관리 4개**: 공지사항(`51:2`, 버튼 보완) / FAQ 관리(`336:22`, 신규, 실제 FAQ 4건) / 이벤트 관리(`339:48`, 신규, 실제 이벤트 2건 — 추첨완료 1건은 Status Badge+"응모자 보기" 버튼, 비추첨 1건은 "-") / 문의내역(`342:82`, 신규, 실제 관리자 조회 데이터 6건 전체를 그대로 반영 — 이 화면은 관리자가 실제로 봐야 하는 운영 데이터라 마이페이지/고객센터의 "깨끗한 예시만" 원칙과 다르게 테스트 흔적 포함 전량 표시).
    - **부수 발견 3 (같은 근본 원인) — Backoffice 탭 칩도 solid/soft 혼동 버그 있었음**: `.bo-product-tab.active`/`.bo-faq-tab.active` 둘 다 CSS상 soft 스타일인데, 기존 "상품미리보기"/"공지사항" 칩이 solid `State=Active`로 잘못 스왑돼 있던 것 추가 발견 — 두 화면 다 새 `State=Active-Soft`(`312:2`)로 즉시 수정(이번에 새로 만든 6개 화면은 처음부터 soft로 제작).
    - **버그**: 이벤트 관리 테이블에서 "응모자 보기"+"수정"+"삭제" 3버튼이 들어가는 액션 칸 폭을 280px로 고정했는데 실제 필요 폭(340px)보다 좁아 버튼이 테이블 밖으로 잘려나가던 것 스크린샷으로 발견 — 기간/추첨 열 폭을 줄여(200→170, 140→110) 액션 열에 여유(340px) 확보.
    - **발견만 하고 미처리(각각 backlog 새 항목으로 기록)**: (1) Backoffice `admin-table`의 `table-header` 배경색이 전 화면(Products/Members 등)에서 공통적으로 `--bg-soft`가 아니라 `--border` 색을 쓰고 있음(코드는 `--bg-soft`) — 광범위하게 반복되는 기존 패턴이라 이번 스코프에서 수정 안 하고 새 테이블도 일단 이 기존 관행을 그대로 따름. (2) 공지사항 관리 패널에 실제 웹에 있는 검색바(`bo-search-inline`)가 아예 빠져있음 — 새로 만든 FAQ/이벤트/문의내역엔 포함시켰지만 공지사항 자체는 이번 스코프 밖.
    - 8개 화면 전부 실제 웹과 스크린샷 대조 검증 완료, 위치 안내 캡션 추가.
  - 로그인/비로그인 상태 분리도 나머지 화면 재점검
- [x] ~~탭 활성 상태 solid/soft 스타일 혼동 버그 (Chip 컴포넌트)~~ (완료, 2026-08-04) — 즐겨찾기 화면을 실제 웹과 대조하다 발견: `.account-tab.active`(마이페이지·내 계좌 탭)는 CSS상 soft 스타일(연한 배경 `--blue-soft`+`--blue-line` 테두리+`--blue` 텍스트)인데, `.cat-tab.active`(상품안내·고객센터 탭)는 solid 스타일(진한 배경+흰 텍스트) — 서로 다른 두 스타일인데 Figma `Chip` 컴포넌트셋(`17:20`)엔 solid `State=Active`(`17:19`) 하나뿐이라, 지금까지 만든 마이페이지/내 계좌 계열 화면 전부가 실제로는 soft여야 할 자리에 solid를 잘못 쓰고 있었음. **조치**: `Chip`에 새 variant `State=Active-Soft`(`312:2`, `--blue-soft`/`--blue-line`/`--blue` 텍스트) 추가 후, 영향받은 8개 화면(`Screens/Account`=`32:2`, `Account - Transfer` STEP1~3=`260:41`/`265:2`/`270:2`, `Account - Transaction Detail`=`279:90`, `MyPage` 보안/계좌관리/즐겨찾기=`301:33`/`304:2`/`306:117`)의 활성 탭 칩을 전부 새 variant로 스왑, 웹과 스크린샷 대조로 재검증 완료. (스왑 도중 즐겨찾기 화면에서 칩 ID를 착각해 계좌관리/즐겨찾기 활성상태가 잠깐 뒤바뀌었던 실수도 즉시 발견해 수정.)
- [x] ~~Figma → 웹사이트(`http://localhost:8000`) 역반영~~ (완료, 2026-08-05) — 착수 전 조사 결과, 지금까지 Figma 작업 중 발견된 불일치는 거의 전부 "Figma가 실제 코드와 달랐던" 케이스였고 Figma 쪽에서 수정해왔음(실제 코드 버그는 발견 즉시 그 자리에서 고쳐온 관행). 확실히 남아있던 코드 쪽 갭은 `CLAUDE.md`에 명시된 "Phase 0 spacing/radius/shadow 토큰(`--space-*`/`--radius-*`/`--shadow-*`)이 정의만 되고 전면 적용은 안 된 상태" 하나였음 — 사용자 확인 후 아래 2단계로 범위 확정해 진행.
  - **Phase 1(토큰 정리)**: `site/css/style.css`의 padding/margin/gap/border-radius/box-shadow 선언 359개 중, 값이 토큰과 **정확히** 일치하는 성분만 `var(--space-N/--radius-*/--shadow-*)`로 치환(Python 스크립트로 성분 단위 판정 — 안 맞는 값은 절대 끼워맞추지 않음). 51개 선언 치환. Home/상품안내/Backoffice 대시보드/마이페이지 4곳 스크린샷 치환 전후 대조 + 콘솔 에러 확인 — 시각적으로 완전히 동일(값 자체는 안 바꾸고 표현만 토큰화한 것이므로 당연한 결과), 회귀 없음 확인.
  - **Phase 2(최근 화면 재검증)**: 마이페이지 리스트 행(`.mp-fav`/`.mp-sched`/`.mp-inquiry` border-bottom 로직, `.mp-sched-badge`, `.mp-stmt-row`), 고객센터·Backoffice 검색창(`.support-search`/`.bo-search-inline`), 탭 활성 스타일(`.account-tab.active` soft/`.cat-tab.active` solid), `.bank-badge`/`.bank-logo` 크기를 Playwright `getComputedStyle`로 재실측해 Figma 반영값과 대조.
    - **발견·수정 1건**: Products(서식·약관·설명서/특별상품 관리)와 FAQ·공지사항관리(FAQ/이벤트/문의내역) — 신규 제작한 Backoffice 서브탭 5개의 검색창/버튼 높이를 36px로 만들었는데, 실제 코드는 `.bo-search-inline input`(`padding:6px`, `.bo-toolbar input`보다 더 구체적인 규칙이 우선 적용)이라 실측 31px이 맞음 — 고객센터 검색창 수정 때 확인한 31px과 실제로 동일한 값인데도 그 이후에 만든 5개 화면엔 반영이 안 돼 있었음. 5개 화면 전부 36→31로 수정, 스크린샷 확인.
    - **그 외 전부 일치 확인**(코드 수정 불필요): 리스트 행 border-bottom 로직(2건 이상 있는 즐겨찾기로 재확인, 마지막 행만 테두리 없음 정상), 탭 활성 색상(soft `#E3F6EC`/`#A8E0C4`/`#0FA968`, solid `#0FA968`+흰 텍스트 전부 정확히 일치), BankBadge 로고 22×22/radius 5px 일치, `admin-table th` 배경색은 이미 알려진 대로 코드(`--bg-soft`)가 맞고 Figma만 틀린 상태 그대로(코드 쪽 조치 불필요).
    - **버그 아닌 발견**: 마이페이지 예약이체 탭의 "지연" 이체 1건이 재검증 시점엔 화면에 안 보였음 — 실제 시간이 지나 `transfer_consumer.py`가 이미 자동 처리(완료 상태로 전환)한 것으로 확인, 실시간 데모 데이터가 자연스럽게 노후화된 사례일 뿐 코드/Figma 버그 아님.
- [ ] 새로 만드는 화면/컴포넌트에 설명 텍스트(로그인 상태·탭/단계·핵심 구성요소 캡션) 계속 추가
  - 2026-07-28 추가한 컴포넌트 variant 7개(`componentVariantsAdded2026_07_28`)엔 아직 캡션 없음
- [x] ~~Products 정렬 필터(`sort-select`) 웹 불일치~~ (완료, 2026-08-06) — 셰브런 아이콘 누락 + 폰트 Regular 13px(실제 SemiBold 14px) + 높이 36px(실제 38px)를 실제 CSS(`.product-sort`) 기준으로 수정. `icon/chevron-down`(`55:86`) 재사용, `--text-sub` 재바인딩. 6개 인스턴스(예금/적금/전세자금대출 "높은순" 3 + 주택담보대출/신용대출 등 "낮은순" 3) 전부 반영. 상세: `session-log.md` `### 2026-08-06 (계속)`.
  - [ ] **`sort-select`가 공유 컴포넌트가 아니라 화면마다 복제된 독립 FRAME** — 이번처럼 나중에 또 어긋날 수 있음. 정식 컴포넌트로 승격할지 여부 사용자 확인 대기 중.

## 2026-07-28 감사(컴포넌트 11개 + 화면 21개) 중 발견됐지만 스코프 밖이라 미처리

디자인시스템 감사(값/구조 불일치)와 별개로, 콘텐츠·데이터 자체가 실제와 다른 항목들. 2026-07-29부로 이 5건이 위 "Figma 디자인시스템 구축" 섹션의 남은 항목(아이디찾기/비밀번호찾기 등)보다 먼저 처리하기로 확정됨.

- [x] ~~Products 상품 리스트 가짜 데이터~~ (완료, 2026-07-29) — 실제 API 상위 8개로 교체(최신 실측: 한국스탠다드차타드 e-그린세이브예금 3.95% → 전북은행 JB123정기예금 3.81% → 광주은행 굿스타트예금 3.79% → 전북은행 JB다이렉트예금통장 3.76% → 수협은행 Sh첫만남우대예금 3.75% → 케이뱅크 코드K정기예금 3.71% → 제주은행 J정기예금 3.70% → 광주은행 미즈월복리정기예금 3.69%, 자연 드리프트로 순서 일부 변동). 겸사겸사 상품명 텍스트가 흰색 리터럴 fill이라 안 보이던 기존 버그도 발견해 `color/text/primary` 변수 바인딩으로 수정. 상세: `session-log.md` `### 2026-07-29`.
- [x] ~~BO_UsageStats "카테고리별 관심도" 구조 문제~~ (완료, 2026-07-29) — RankedBar 3→6슬롯 확장. 작업 전 라이브 재측정(admin API 직접 호출): 예금123/적금80/금리비교34/신용대출30/주택담보대출28/전세자금대출25(총320건) — 기존 Figma값(예금117/적금89/주택담보대출55, 3개뿐)과 비교해 순위 변동까지 있었음(주택담보대출 3위→5위, 금리비교가 새로 3위 진입). RankedBar 컴포넌트는 애초에 "3슬롯 고정"이 아니라 단일 행 컴포넌트라 인스턴스만 3개 더 추가. 상세: `session-log.md` `### 2026-07-29`.
- [x] ~~BO_UsageStats 조회/검색 비중 재캡처~~ (완료, 2026-07-30) — 라이브 재측정: 조회286/검색70(총356건, 80%/20%)으로 갱신. 겸사겸사 이 패널의 RankedBar 2개도 카테고리별 관심도와 동일한 `resize()` 무시 버그(막대폭이 둘 다 84px로 고정)가 있던 걸 발견해 `detachInstance()`로 같이 수정. 상세: `session-log.md` `### 2026-07-30`.
- [x] ~~BO_PromptMgmt A/B 비교 예시 답변 재검토~~ (완료, 2026-07-30) — 조사 결과 실제 응답 로그가 아니라 랜딩 페이지(`site/index.html:207-208`) 데모 문구를 재사용한 순수 목업으로 확정. 웹은 원본 유지, Figma만 가상 은행명("가나은행")으로 교체(실제 Products TOP추천 데이터와 완전히 겹쳐 오인 소지 있었음) — 웹·Figma가 의도적으로 다른 유일한 케이스라 노드 태깅 + 상태 원장 `knownIssues`에 재감사 오탐 방지 등록까지 완료. 상세: `session-log.md` `### 2026-07-30`.
- [x] ~~BO_UsageStats/BO_Products "카테고리별 인기 상품 TOP 5" 패널 콘텐츠 누락~~ (완료, 2026-07-30) — `Topcat Item` 컴포넌트 재사용, Products 화면의 검증된 25개 인스턴스 값 그대로 복사(API 재호출 안 함). 실제 CSS 실측(1180px, 인셋 없음)으로 배치 — 기존 32px 인셋 패널들과는 의도적으로 안 맞는 임시 상태(아래 항목 참고). BO_Products는 "인기 상품 통계" 탭 콘텐츠 자체가 없어서 중첩 패널 구조(`FSS 상품 조회 통계`+`카테고리별 인기 상품`)까지 새로 만들었고, 프레임 높이가 콘텐츠에 안 맞아 마지막 항목이 잘리던 것도 864→973px로 재조정. 부수적으로 Products 원본 TOP5 패널 자체의 그리드 gap 버그(24px, 실제 CSS 16px라 5개 컬럼이 992px 프레임을 54px 오버플로우하던 것)도 같이 발견·수정(FILL 사이징으로 전환, 992px에 정확히 맞춤).

## BankBadge 컴포넌트 적용 (2026-07-29 신규 발견 — Products 화면 재검토 중)

Products 특별상품/TOP추천/상품리스트에 은행 약칭 배지(`.bank-badge`)가 Figma에 없던 걸 발견해 `Components / Badge & Status`(`159:34`)에 컴포넌트 신설 완료. 같은 패턴(배지 누락/placeholder)이 반복되는 곳들 — 상세는 `session-log.md` 해당 날짜 참고.

- [x] ~~BankBadge 컴포넌트 신설~~ (완료) — 16 variant, `.bank-badge` 실측값 그대로
- [x] ~~Products 특별상품/TOP추천/상품리스트 3영역 적용~~ (완료) — 겸사겸사 SC제일은행/한국스탠다드차타드은행 이름 불일치로 라이브 사이트에서도 배지가 잘못 뜨던 버그를 `fss_fetcher.py`에서 수정
- [x] ~~Products "은행 바로가기" 그리드~~ (완료) — 8개 칩 전부 배지 추가
- [x] ~~Account 화면 계좌 카드(`account-grid`)~~ (완료) — 빈 `Ellipse` 2곳을 BankBadge 인스턴스로 교체(`.acct-bank-logo .bank-badge` 36px 확대 버전에 맞춰 인스턴스 단위 오버라이드)
- [ ] **← 다음은 여기부터.** **BankBadge 적용 필요 (미확인)** — Backoffice 회원관리/상품관리 테이블 등에서 `bankBadge()`를 쓰는지 먼저 확인 후 진행 (이번 스코프에서 의도적으로 제외)
- [x] ~~BankBadge에 `Bank=아이엠뱅크` variant 누락~~ (완료, 2026-08-05 — "미생성"으로 결론) — 코드(`BANK_BRAND`)엔 아이엠뱅크가 대구은행과 동일 색상(#EE7D1F)/동일 로고(`daegu.png`)로 등록돼 있음을 재확인. 별도 variant를 만드는 대신, 이미 같은 상황인 `KB국민은행`↔`국민은행`(`Bank=KB` 공유)/`NH농협은행`↔`농협은행`(`Bank=NH` 공유)과 동일하게 **기존 `Bank=대구` variant를 그대로 재사용**하기로 결정(상세 경위: `session-log.md` 2026-08-05 항목). 아이엠뱅크가 화면에 등장하면 `Bank=대구`로 스왑하면 됨 — 더 이상 "variant 누락" 상태 아님.
- [x] ~~BankBadge 컴포넌트에 "로고 이미지" variant 추가~~ (완료, 2026-08-05) — 기존 16개 variant를 `Bank=X, Style=Badge`로 리네임하고, 동일 16개 은행에 대해 `Bank=X, Style=Logo`(22×22, radius5, 흰 배경) variant를 신규 생성 후 `upload_assets`로 `site/img/banks/`의 실제 PNG 16개를 각 노드에 fill로 업로드(`scaleMode:"FIT"`, 웹의 `object-fit:contain`과 동일). 컴포넌트셋이 `Bank`(16)×`Style`(Badge/Logo 2) = 32 variants로 정상 인식됨(스크린샷으로 순서·이미지 일치 확인).
  - [x] ~~파일 전체 `BankBadge` 인스턴스 97개를 `Style=Logo`로 스왑~~ (완료, 2026-08-05) — Account 6개/Products 88개/MyPage 3개 전부 `swapComponent()`로 일괄 전환(Bank 값 매칭, 스킵 0건). Account/Products/MyPage(계좌관리) 스크린샷으로 실제 로고 렌더링 확인.
  - [x] ~~수협/산업은행(KDB)/씨티은행 3개 Bank variant 추가~~ (완료, 2026-08-05) — 각각 `Bank=수협`/`Bank=KDB`/`Bank=씨티`로 Badge(색상 배지, 기존 16개와 동일 레시피: `layoutMode:HORIZONTAL, primaryAxisSizingMode:AUTO, counterAxisSizingMode:FIXED, padding 5/5/0/0, cornerRadius:6, h:22`)+Logo(22×22, `site/img/banks/`의 `suhyup.png`/`kdb.png`/`citibank.png` 업로드) 2종씩 총 6개 신규 생성. 컴포넌트셋이 `Bank`(19)×`Style`(2)=38 variants로 정상 인식, 스크린샷 확인 완료.
    - **`아이엠뱅크`는 별도 variant 미생성** — `BANK_BRAND`상 label/color/logo가 대구은행과 완전히 동일(`{label:"대구", color:"#EE7D1F", logo:"daegu.png"}`)이라, 기존에 이미 같은 방식으로 처리된 `KB국민은행`↔`국민은행`(`Bank=KB` 공유)·`NH농협은행`↔`농협은행`(`Bank=NH` 공유) 사례와 동일하게 **기존 `Bank=대구` variant를 그대로 재사용**하는 게 일관된 설계라고 판단 — 아이엠뱅크로 표기된 화면/인스턴스가 생기면 `Bank=대구`로 스왑하면 됨(처음엔 사용자 요청대로 별도 variant를 만들었다가, 이 중복 문제를 발견하고 되돌림).
- MyPage 계좌 목록의 배지는 별도 이슈 아님 — 위 "화면 커버리지를 메뉴칩/플로우 단계별로 전부 확장" 항목(계좌 관리 서브탭 자체 미완성)에 포함되는 문제라 여기 중복 기재 안 함

## 실사이트 버그 수정 이력 (디자인시스템 작업 스코프 밖 — 코드 데이터 로직)

Figma 작업 중 발견됐지만 Figma가 아니라 실제 앱 코드(`fss_fetcher.py`/`site/js/main.js`)를 고친 건들. 디자인시스템 작업과 구분해서 여기 별도 기록.

- [x] ~~SC제일은행/한국스탠다드차타드은행 은행명 불일치~~ (완료, 2026-07-29) — FSS 법정명("한국스탠다드차타드은행")이 `BANK_BRAND`/URL맵의 "SC제일은행"과 안 맞아 예금 1위 상품 배지가 회색 폴백으로 잘못 표시되던 버그. `fss_fetcher.py`의 `_BANK_NAME_ALIASES`에 정규화 추가
- [x] ~~중소기업은행/IBK기업은행 은행명 불일치~~ (완료, 2026-07-29) — IBK의 법정명("중소기업은행")이 매핑 안 돼 배지·공식상품페이지 링크 둘 다 깨져있던 것 확인, 같은 방식(`_BANK_NAME_ALIASES`)으로 정규화
- [x] ~~아이엠뱅크(구 대구은행) 배지 누락~~ (완료, 2026-07-29) — 대구은행이 iM뱅크로 리브랜딩되며 FSS가 새 법정명 반환, `BANK_BRAND`엔 옛 이름("대구은행")만 있어 배지 누락. **주의**: `_BANK_PRODUCT_URLS`엔 반대로 새 이름("아이엠뱅크")만 있어서 SC/IBK와 같은 방식(Python alias)으로 고치면 URL 링크가 깨짐 — 그래서 `site/js/main.js` `BANK_BRAND`에 "아이엠뱅크" 키를 별도 추가하는 방식으로 처리(기존 "대구은행" 키는 유지)
- [ ] **(미확인, 발견만 함)** `_BANK_PRODUCT_URLS`(`fss_fetcher.py`)에 "KB국민은행"/"NH농협은행" 키만 있는데 FSS는 "국민은행"/"농협은행"(축약형)을 반환 — 배지는 `BANK_BRAND`에 두 표기가 다 있어 안 깨지지만, 공식 상품 페이지 링크(`url` 필드)는 이 두 은행에서 계속 비어있을 가능성 높음. 위 3건과 같은 검증 방법(카테고리 5개 전수 조회 후 대조)으로 확인 후 필요하면 `_BANK_PRODUCT_URLS`에 축약형 키 추가
- 매핑 자체가 아예 없는 은행(별칭 대상 아님, 신규 브랜드 항목 추가가 필요하면 별도 논의): 수협은행, 한국산업은행(KDB), 한국씨티은행
- [x] ~~은행 로고 이미지 16종 추가(`site/img/banks/`)~~ (완료, 2026-08-04) — Figma `Foundations / Banks`에 사용자가 추가한 로고 라이브러리에서 `BANK_BRAND`(`main.js`) 파일명 규칙에 맞는 16개 export(PNG, 288×288, `download_assets` 도구 사용) 후 `site/img/banks/`에 추가. `bankBadge()`가 이미 `<img onerror=...>` 폴백 구조로 짜여 있어 **코드 수정 없이 파일 추가만으로 로고 이미지 우선 표시가 자동 작동** 확인(계좌/상품안내/은행 바로가기 등 스크린샷 대조, 콘솔 404 없음). 로고 없는 3개 은행(수협은행/한국산업은행/한국씨티은행)은 기존대로 색상 배지 폴백 유지 확인.
  - **주의(사용자 판단 필요)**: `Foundations / Banks` 라이브러리의 심볼명이 "신한-제주은행"처럼 지주사 단위로 묶여 있어, `busan.png`=`gyeongnam.png`(BNK금융지주 공유 마크, 실제로 두 은행 다 BNK 브랜드 사용 — 타당), `gwangju.png`=`jeonbuk.png`(JB금융지주 공유 마크 — 타당)까지는 실제로도 그럴듯하지만, `jeju.png`가 `shinhan.png`와 완전히 동일한 이미지(신한 타이거 마크)를 그대로 재사용하는 건 실제 제주은행 로고와 다를 가능성이 높음(신한금융지주가 제주은행 대주주인 건 맞지만, 제주은행은 실제로 자체 로고를 씀) — 이 파일만 원치 않으면 삭제하고 색상 배지 폴백으로 되돌릴 것.
  - [x] ~~수협은행/한국산업은행/한국씨티은행 3개 은행도 로고 매핑 추가~~ (완료, 2026-08-04) — 사용자가 Figma 팩의 `수협`(`250:802`)/`산업은행`(`250:760`)/`시티은행`(`250:861`) 심볼로 지정 확인. `download_assets`로 export(`suhyup.png`/`kdb.png`/`citibank.png`, 288×288) 후 `site/img/banks/`에 추가, `main.js`의 `BANK_BRAND`에 3개 신규 항목 추가(코드 수정 발생 — 기존 16개와 달리 이 3개는 원래 매핑 자체가 없었으므로 불가피): `수협은행`→`{label:"수협", color:"#0C4DA2", logo:"suhyup.png"}`, `한국산업은행`→`{label:"KDB", color:"#1B3F94", logo:"kdb.png"}`, `한국씨티은행`→`{label:"씨티", color:"#EC1C24", logo:"citibank.png"}`. 신용대출 카테고리(3개 은행 전부 등장) 실측으로 로고 전환 확인 — 이걸로 사이트에 등장하는 19개 은행 전부 로고 이미지 적용 완료, 색상 배지 폴백이 필요한 은행 없음.
  - **`jeju.png`=`shinhan.png` 재사용 건**: 사용자 확인 결과 "제주은행이 실제로 신한 마크를 쓰고 있는 게 맞다"고 확인 — 그대로 유지.
